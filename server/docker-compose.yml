services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: echo
      POSTGRES_PASSWORD: echo
      POSTGRES_DB: echo
    ports:
      - '5433:5432'
    volumes:
      - echo_db:/var/lib/postgresql/data

volumes:
  echo_db:
