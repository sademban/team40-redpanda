/**
 * Video background configuration
 * Centralized video asset paths and properties
 */

export const videoConfig = {
  source: '/338904.mp4',
  type: 'video/mp4',
  opacity: 0.4, // 0-1 range for visibility
  fallbackText: 'Your browser does not support the video tag.',
}
